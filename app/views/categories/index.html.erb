<h3> Total categories : <span id="category_count"><%= @categories.size %> </span>

</h3>

<ul id="categories">
<% for category in @categories %>
<li id="li_<%= category.id %>"><%= category.name %> <%= link_to "destroy" ,category_path(category), method: :delete,data: {
    	confirm: "Are you sure?"},remote: true %></li>
<%end%>
</ul>

<%= link_to "Add category" ,new_category_path, id: "new_category_link" , remote: true%>